"""Default values that depend on note types in one direction."""


print("note defaults module started")

from . import p4_3_note_types as note_types

print(f"default kind: {note_types.DEFAULT_KIND}")

print("note defaults module finished")
